{
  "name": "ts-decontainer",
  "version": "1.1.0",
  "description": "Creates ts decorator and grabs all same type decorator outputs",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "dev": "nodemon --watch ./src/**/*.ts --exec ts-node ./src/index.ts",
    "start": "ts-node dist/server.js",
    "build": "tsc",
    "prepare": "npm run build",
    "postversion": "git push && git push --tags"
  },
  "author": "Fatih Cüre",
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3",
    "nodemon": "^2.0.2"
  }
}
